/*eslint-disable*/
import React from 'react';
import { useHistory } from 'react-router';
import { observer } from 'mobx-react-lite';

const GeneratedKey = observer(() => {
  const history = useHistory();
  return <div>GeneratedKey</div>;
});
export default GeneratedKey;
